# contract-api-log-analysis

The contract for api-log-analysis.

This is a basic demo API that allows you to provide an input log file and receive back analysis on
- The number of unique IP addresses
- The top 3 most visited URLs
- The top 3 most active IP addresses

## Dependencies
- Java 21
- Maven

## Usage
To install locally run
```bash
mvn clean install
```